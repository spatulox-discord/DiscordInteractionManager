{
  "name": "@spatulox/discord-interaction-manager",
  "version": "2.0.6",
  "author": "Spatulox",
  "description": "discord-interaction-manager is a lightweight CLI tool to manage Discord bot interactions (slash commands, context menus) in under 30 seconds. Deploy, update, delete, and generate interaction files with an interactive terminal interface.",
  "bin": {
    "dim": "dist/MainCLI.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/spatulox-discord/DiscordInteractionManager.git"
  },
  "bugs": {
    "url": "https://github.com/spatulox-discord/DiscordInteractionManager/issues"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "build": "rm -rf dist/ && tsup",
    "patch": "npm run build && npm version patch",
    "minor": "npm run build && npm version minor",
    "major": "npm run build && npm version major",
    "pub:patch": "npm run patch && npm publish --access public",
    "pub:minor": "npm run minor && npm publish --access public",
    "pub:major": "npm run major && npm publish --access public",
    "cli": "node dist/MainCLI.js",
    "dev": "npm run build && npm run cli"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "dotenv": "^17.3.1"
  },
  "peerDependencies": {
    "discord.js": "^14.26.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "tsup": "^8.5.1",
    "tsx": "^4.20.3",
    "typescript": "^5.9.3"
  },
  "keywords": [
    "discord",
    "slash commands",
    "context menu",
    "interaction",
    "cli"
  ]
}
