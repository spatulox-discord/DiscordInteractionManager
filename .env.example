DISCORD_BOT_TOKEN=example
# Optional: folder where the interaction files are stored (default: ./handlers)
DISCORD_INTERACTION_FOLDER=./handlers
# Optional: "true" uses the *_dev folders (commands_dev, context_menu_dev)
DISCORD_BOT_DEV=false
